module.exports = {
  /**
   * 环境变量
   * @description 这个对象定义了环境变量。
   */
  env: {
    /**
     * NODE_ENV 环境变量
     * @description 指定当前环境为开发环境。
     */
    NODE_ENV: '"development"'
  },
  /**
   * 定义常量
   * @description 这个对象定义了常量。
   */
  defineConstants: {
    // 这里可以定义常量
  },
  /**
   * 小程序配置
   * @description 这个对象定义了小程序的配置。
   */
  mini: {
    // 这里可以定义小程序的配置
  },
  /**
   * H5配置
   * @description 这个对象定义了H5的配置。
   */
  h5: {
    // 这里可以定义H5的配置
  }
}
